<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="#BEBEBE"
    android:layout_marginTop="10dp">

    <!-- Ảnh -->

    <!-- Đoạn văn bản -->
    <ImageView
        android:id="@+id/imageView"
        android:layout_width="125dp"
        android:layout_height="125dp"
        android:layout_alignParentLeft="true"
        android:layout_marginLeft="286dp"
        android:layout_alignParentTop="true"
        android:src="@mipmap/mission5image"
        android:scaleType="fitXY"/>

    <TextView
        android:id="@+id/textView"
        android:layout_width="286dp"
        android:layout_height="wrap_content"
        android:fontFamily="@font/inter_extrabold"
        android:textStyle="bold"
        android:text="The James Webb Space Telescope is an infrared observatory orbiting the Sun about 1 million miles from Earth to find the first galaxies that formed in the early universe and to see stars forming planetary systems."
        app:layout_constraintEnd_toStartOf="@+id/imageView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/imageView" />

    <!-- Nút -->
    <Button
        android:id="@+id/button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Detail >>"
        android:textColor="#1914FF"
        android:textSize="17dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_marginTop="90dp"
        android:background="@android:color/transparent"
        />

</RelativeLayout>